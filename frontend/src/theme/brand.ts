/** Logo asset served from `frontend/public` (Vite root URL). */
export const clubLogoSrc = '/brand/mk.png'

/**
 * Background blue from the outer field of `mk.png` (royal / medium-dark blue).
 * Approx. HSL hue ~209° (cyan-leaning blue on the wheel).
 */
export const clubNavBlue = '#2c6eb5'
